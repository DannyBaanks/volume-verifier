# Volume Verifier - checksums
#
# The published executable was built from the source in this repository.
# Rebuild from source and compare behavior (see TESTING.md).
#
# SHA-256 of the published binary (volume-verifier.exe, 6,525,253 bytes):
bd288f2eb770316a8aadde6d3ede64103d647d04a91da064d9008b34f55268b1  volume-verifier.exe

# Reference build produced from source/volume_verifier.py with build.ps1
# (PyInstaller 6.22.0, Python 3.12.4, Windows) during repository setup.
# NOTE: PyInstaller embeds build metadata (timestamps, versions), so a fresh
# build is NOT byte-for-byte identical to the published binary. Verify by
# rebuilding and comparing behavior, not by comparing hashes.
0a3105cc02d41fa72605b63e21c23b92de6392c0beabe010b895bb8470e39f59  dist/volume-verifier.exe (reference build)
